/**
 * 6ft AimPoint Combine — 10 putts × 4 readings (33% / 66% guess vs actual).
 * Saved as one practice row: test_type aimpoint_6ft_combine, metadata JSONB.
 */
export const aimpoint6ftCombineConfig = {
  testName: "6ft AimPoint Combine",
  testType: "aimpoint_6ft_combine",
  noteKind: "AimPoint6ftCombine",
  puttCount: 10,
  distanceFt: 6,
} as const;
